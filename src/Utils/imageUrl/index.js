const imageUrl = (url) => {
    return url
    // fetch(url)
    //     .then((res) => res.json())
    //     .then((response) => {
    //         return JSON.stringify(response.sprites)
        
    // })

}

export default imageUrl
